import { Styles } from '@ijstech/components';

Styles.cssRule('#pnlRandomizerMain', {
  margin: '0 auto',
  maxWidth: '1400px',
  $nest: {
    '.random-number': {
      textAlign: 'center'
    }
  }
});
